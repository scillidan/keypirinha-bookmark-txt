[main]
# Search mode: keyword (default), direct, full
mode = keyword
keyword = bkm
comment_prefix = ##
max_desc_len = 20% (or 60)
directories = 
    ${env:USERHOME}/bookmarks;
    ${env:USERHOME}/bookmarks_others
patterns = *.bkm
ignore = *.txt;arch
browser_args = open-cli